spring:
  profiles:
    include:
      - credential
  datasource:
    url: jdbc:h2:mem:test
    username: sa
    password:
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        default_batch_fetch_size: 100
    open-in-view: false
  jwt:
    secret:
      test

janus:
  admin:
    secret: test
    server: test
  room:
    secret: test

cookie:
  secure:
    false
  domain:
